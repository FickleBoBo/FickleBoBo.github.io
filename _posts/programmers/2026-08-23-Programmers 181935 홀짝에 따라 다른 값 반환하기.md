---
title: "[Programmers] #181935 - 홀짝에 따라 다른 값 반환하기 [Java][C++][Python]"
date: 2026-08-23
categories: [PS, Programmers]
tags: ["warm up"]
description: "n이 홀수면 이하 홀수의 합을, 짝수면 이하 짝수의 제곱합을 반환하는 워밍업 문제."
slug: programmers-181935
media_subpath: /assets/img/posts/programmers-181935/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181935)
{: .prompt-info }

---

## 1. 아이디어

양의 정수 `n`이 홀수면 1부터 `n`까지의 홀수를 모두 더한 값을, 짝수면 2부터 `n`까지의 짝수를 각각 제곱해 더한 값을 반환하면 되는 문제다. 홀짝에 따라 순회 시작값과 누적하는 값(그 자체 또는 제곱)만 다를 뿐 구조는 동일해서, 두 경우 모두 2씩 증가하는 반복문으로 값을 누적했다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(1)$ |

($N$ = 입력값 `n`)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public int solution(int n) {
        if (n % 2 == 1) {
            int sum = 0;
            for (int i = 1; i <= n; i += 2) {
                sum += i;
            }
            return sum;
        } else {
            int sum = 0;
            for (int i = 2; i <= n; i += 2) {
                sum += i * i;
            }
            return sum;
        }
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int solution(int n) {
    if (n % 2) {
        int sum = 0;
        for (int i = 1; i <= n; i += 2) {
            sum += i;
        }
        return sum;
    } else {
        int sum = 0;
        for (int i = 2; i <= n; i += 2) {
            sum += i * i;
        }
        return sum;
    }
}
```

```python
def solution(n):
    if n % 2:
        return sum(range(1, n + 1, 2))
    else:
        return sum(i * i for i in range(2, n + 1, 2))
```

---

## 참고

반복문 없이 닫힌 형태 수식으로도 구할 수 있다. 1부터 연속된 홀수 $k$개의 합은 항상 $k^2$이므로, `n`이 홀수면 $k = (n+1)/2$일 때 홀수 합은 $k^2$이다. `n`이 짝수면 $m = n/2$일 때 짝수 제곱합은 $2m(m+1)(2m+1)/3$이다 — 짝수 $2, 4, \dots, n$의 제곱합은 $4(1^2 + 2^2 + \dots + m^2)$과 같고, 제곱합 공식 $m(m+1)(2m+1)/6$에 4를 곱해 정리한 값이다.

---

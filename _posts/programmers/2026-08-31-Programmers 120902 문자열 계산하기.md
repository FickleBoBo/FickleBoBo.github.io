---
title: "[Programmers] #120902 - 문자열 계산하기 [Java][C++][Python]"
date: 2026-08-31
categories: [PS, Programmers]
tags: ["string"]
description: "덧셈과 뺄셈만 등장하는 수식 문자열을 계산해 결과를 구하는 문제."
slug: programmers-120902
media_subpath: /assets/img/posts/programmers-120902/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/120902)
{: .prompt-info }

---

## 1. 아이디어

수식은 공백으로 구분된 `숫자 연산자 숫자 연산자 ...` 형태다. 첫 숫자를 초깃값으로 두고 이어지는 `연산자 숫자` 쌍을 왼쪽부터 차례로 반영한다 — `+`면 더하고 `-`면 뺀다. `-` 뒤의 항을 음수로 보면 부호를 반영한 모든 항의 합으로도 계산할 수 있다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `my_string`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.util.*;

class Solution {
    public int solution(String my_string) {
        StringTokenizer st = new StringTokenizer(my_string);
        int ans = Integer.parseInt(st.nextToken());

        while (st.hasMoreTokens()) {
            String op = st.nextToken();
            if (op.equals("+")) {
                ans += Integer.parseInt(st.nextToken());
            } else {
                ans -= Integer.parseInt(st.nextToken());
            }
        }

        return ans;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int solution(string my_string) {
    stringstream ss(my_string);
    int ans, x;
    char op;

    ss >> ans;
    while (ss >> op >> x) {
        ans += op == '+' ? x : -x;
    }

    return ans;
}
```

```python
def solution(my_string):
    return sum(map(int, my_string.replace("+ ", "").replace("- ", "-").split()))
```

`replace`로 `"+ "`를 지우고 `"- "`를 `"-"`로 바꾸면 연산자가 사라지고 각 숫자가 부호를 가진 형태만 남는다. `split`으로 나눈 뒤 전부 정수로 바꿔 더하면 수식의 값이 된다.

---

---
title: "[백준] 11720번 - 숫자의 합 [Java][C++]"
date: 2025-12-04
categories: [PS, BaekJoon]
tags: [구현, 수학, 문자열]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/11720)

---

## 1. 문제 풀이

주어진 $N$ 개의 숫자에 대해 각 자릿수의 합을 구하는 문제로, 주어진 숫자들을 문자열로 입력 받은 후 각 자리의 수를 더하는 방식으로 해결하면 된다.

---

## 2. 코드

### 1. 구현 [Java]

문자열의 각 자리 문자에 대해 `'0'` 을 빼면 실제 정수를 얻을 수 있다.

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        br.readLine();
        String str = br.readLine();

        int sum = 0;
        for (char c : str.toCharArray()) {
            sum += c - '0';
        }

        System.out.println(sum);
    }
}
```

### 2. 구현 [C++]

문자열의 각 자리 문자에 대해 `'0'` 을 빼면 실제 정수를 얻을 수 있다.

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    string s;
    cin >> n >> s;

    int sum = 0;
    for (char c : s) {
        sum += c - '0';
    }

    cout << sum;
}
```

---

## 3. 풀이 정보

### 1. 구현 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 104 ms | 14188 KB | 390 B     |

### 2. 구현 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2024 KB | 251 B     |

---

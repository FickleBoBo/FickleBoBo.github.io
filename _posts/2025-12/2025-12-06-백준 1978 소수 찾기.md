---
title: "[백준] 1978번 - 소수 찾기 [Java][C++]"
date: 2025-12-06
categories: [PS, BaekJoon]
tags: [수학, 정수론, 소수 판정, 에라토스테네스의 체]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/1978)

---

## 1. 문제 풀이

주어진 수 중에서 소수의 개수를 구하는 문제로 각 수에 대해 $2$ 부터 해당 수의 제곱근까지 나누어서 나머지가 $0$ 이 되는 수가 있는지 판단하는 방식으로 찾아도 되고, 에라토스테네스의 체를 활용해서 미리 소수를 구한 후 비교하는 방식으로도 해결할 수 있다.

---

## 2. 코드

### 1. 소수 판정 [Java]

반복문 라벨링을 활용해서 소수가 아닐 시 해당 수에 대한 판정은 빠른 종료를 해줬다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[] arr = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        int cnt = 0;
        out:
        for (int n : arr) {
            if (n == 1) continue;

            for (int i = 2; i * i <= n; i++) {
                if (n % i == 0) continue out;
            }

            cnt++;
        }

        System.out.println(cnt);
    }
}
```

### 2. 에라토스테네스의 체 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[] arr = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        boolean[] isPrime = sieve(1000);
        int cnt = 0;

        for (int n : arr) {
            if (isPrime[n]) cnt++;
        }

        System.out.println(cnt);
    }

    static boolean[] sieve(int N) {
        boolean[] isPrime = new boolean[1 + N];
        Arrays.fill(isPrime, 2, N + 1, true);

        for (int i = 2; i * i <= N; i++) {
            if (isPrime[i]) {
                for (int j = i * i; j <= N; j += i) {
                    isPrime[j] = false;
                }
            }
        }

        return isPrime;
    }
}
```

### 3. 소수 판정 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    int cnt = 0;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;

        bool flag = true;

        if (x == 1) flag = false;

        for (int i = 2; i * i <= x; i++) {
            if (x % i == 0) {
                flag = false;
                break;
            }
        }

        if (flag) cnt++;
    }

    cout << cnt;
}
```

### 4. 에라토스테네스의 체 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

vector<bool> sieve(int n) {
    vector<bool> isPrime(1 + n);
    fill(isPrime.begin() + 2, isPrime.end(), true);

    for (int i = 2; i * i <= n; i++) {
        if (isPrime[i]) {
            for (int j = i * i; j <= n; j += i) {
                isPrime[j] = false;
            }
        }
    }

    return isPrime;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<bool> isPrime = sieve(1000);
    int cnt = 0;

    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;

        if (isPrime[x]) cnt++;
    }

    cout << cnt;
}
```

---

## 3. 풀이 정보

### 1. 소수 판정 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 108 ms | 14200 KB | 745 B     |

### 2. 에라토스테네스의 체 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 100 ms | 14268 KB | 1019 B    |

### 3. 소수 판정 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2020 KB | 491 B     |

### 4. 에라토스테네스의 체 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2020 KB | 644 B     |

---

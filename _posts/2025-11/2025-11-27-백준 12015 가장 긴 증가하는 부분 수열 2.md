---
title: "[백준] 12015번 - 가장 긴 증가하는 부분 수열 2 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [다이나믹 프로그래밍, LIS, 이분 탐색]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/12015)

![](/assets/posts/2025-11/백준%2012015%20가장%20긴%20증가하는%20부분%20수열%202/photo1.png)
![](/assets/posts/2025-11/백준%2012015%20가장%20긴%20증가하는%20부분%20수열%202/photo2.png)

---

## 문제 풀이

[백준 11053번 - 가장 긴 증가하는 부분 수열]({% post_url 2025-11/2025-11-27-백준 11053 가장 긴 증가하는 부분 수열 %}) 문제에서 수열의 크기가 커진 문제로 기존의 2중 반복문 기반 LIS로는 시간 내에 해결할 수 없다. [이분 탐색을 활용한 LIS]({% post_url 2025-11/2025-11-27-이분 탐색을 활용한 LIS (Longest Increasing Subsequence With Binary Search) %}) 를 통해 해결할 수 있다.

---

## 코드

### 1. 이분 탐색 LIS [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[] arr = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        int[] dp = new int[1 + N];
        int len = 0;

        for (int i = 0; i < N; i++) {
            int idx = lowerBound(dp, len, arr[i]);

            dp[idx] = arr[i];
            if (idx == len + 1) len++;
        }

        System.out.println(len);
    }

    private static int lowerBound(int[] arr, int len, int key) {
        int left = 1;
        int right = len + 1;

        while (left < right) {
            int mid = (left + right) / 2;

            if (arr[mid] < key) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }

        return right;
    }
}
```

---

## 풀이 정보

### 1. 이분 탐색 LIS [Java]

- 10 min

![](/assets/posts/2025-11/백준%2012015%20가장%20긴%20증가하는%20부분%20수열%202/photo3.png)

---

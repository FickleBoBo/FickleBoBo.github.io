---
title: "[백준] 12738번 - 가장 긴 증가하는 부분 수열 3 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [다이나믹 프로그래밍, LIS, 이분 탐색]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/12738)

![](/assets/posts/2025-11/백준%2012738%20가장%20긴%20증가하는%20부분%20수열%203/photo1.png)
![](/assets/posts/2025-11/백준%2012738%20가장%20긴%20증가하는%20부분%20수열%203/photo2.png)

---

## 문제 풀이

[백준 12015번 - 가장 긴 증가하는 부분 수열 2]({% post_url 2025-11/2025-11-27-백준 12015 가장 긴 증가하는 부분 수열 2 %}) 문제에서 수의 범위가 더 넓어지고 음수까지 포함된 문제로 이분 탐색을 활용한 LIS에서 dp 배열의 초기값을 수열의 가장 작은 수보다 작게 설정해줘야 된다. `0`으로 초기화된 배열을 그대로 쓸 경우 `0`을 실제 값으로 인식하여 잘못된 갱신이 발생하게 된다.

---

## 코드

### 1. 이분 탐색 LIS [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[] arr = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        int[] dp = new int[1 + N];
        dp[0] = Integer.MIN_VALUE;
        int len = 0;

        for (int i = 0; i < N; i++) {
            int idx = lowerBound(dp, len, arr[i]);

            dp[idx] = arr[i];
            if (idx == len + 1) len++;
        }

        System.out.println(len);
    }

    private static int lowerBound(int[] arr, int len, int key) {
        int left = 1;
        int right = len + 1;

        while (left < right) {
            int mid = (left + right) / 2;

            if (arr[mid] < key) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }

        return right;
    }
}
```

---

## 풀이 정보

### 1. 이분 탐색 LIS [Java]

- 1 min

![](/assets/posts/2025-11/백준%2012738%20가장%20긴%20증가하는%20부분%20수열%203/photo3.png)

---

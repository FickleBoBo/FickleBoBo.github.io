---
title: "[Programmers] #181915 - 글자 이어 붙여 문자열 만들기 [Java][C++][Python]"
date: 2026-08-25
categories: [PS, Programmers]
tags: ["string"]
description: "인덱스 배열이 가리키는 위치의 글자를 순서대로 이어 붙여 문자열을 만드는 문제."
slug: programmers-181915
media_subpath: /assets/img/posts/programmers-181915/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181915)
{: .prompt-info }

---

## 1. 아이디어

`index_list`를 순서대로 순회하며 `my_string`에서 해당 인덱스의 문자를 하나씩 이어 붙이면, 인덱스가 가리키는 순서대로 재배열된 문자열을 만들 수 있다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(M)$ | $O(M)$ |

($M$ = `index_list`의 길이, 결과 문자열의 길이도 $M$이므로 공간 역시 $O(M)$이다)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public String solution(String my_string, int[] index_list) {
        StringBuilder sb = new StringBuilder();
        for (int i : index_list) {
            sb.append(my_string.charAt(i));
        }

        return sb.toString();
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

string solution(string my_string, vector<int> index_list) {
    string s;
    for (int i : index_list) s += my_string[i];
    return s;
}
```

```python
def solution(my_string, index_list):
    return "".join(my_string[i] for i in index_list)
```

---

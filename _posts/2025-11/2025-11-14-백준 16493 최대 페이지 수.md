---
title: "[백준] 16493번 - 최대 페이지 수 [Java]"
date: 2025-11-14
categories: [PS, BaekJoon]
tags: [브루트 포스 알고리즘, 백트래킹, 다이나믹 프로그래밍, 배낭 문제]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/16493)

![](/assets/posts/2025-11/백준%2016493%20최대%20페이지%20수/photo1.png)
![](/assets/posts/2025-11/백준%2016493%20최대%20페이지%20수/photo2.png)

---

## 문제 풀이

`M`개의 챕터가 있고 각 챕터는 읽는데 소요되는 일 수와 페이지 수가 주어졌을 때, `N`일 동안 읽을 수 있는 최대 페이지 수를 구하는 문제다. `M`이 최대 `20`이라서 멱집합을 활용한 완전 탐색($2^{20} = 1,048,576$ 가지)으로도 풀 수 있고, 0-1 배낭 문제를 활용하여 각 챕터를 읽을지 안 읽을지 선택하는 방식도 가능하다.

---

## 코드

### 1. 브루트 포스 [Java]

모든 부분 집합을 탐색하기 위해 `powerSet` 메서드를 작성한 후 재귀마다 `idx`를 1씩 증가시키며 해당 챕터를 읽는 경우와 안 읽는 경우에 대한 재귀를 했다. 파라미터로는 현재까지 읽는데 소요된 일 수의 합과 페이지 수의 합을 넘겨서 소요된 일수의 합이 `N`보다 크면 해당 케이스는 불가능한 경우로 판단하고 리턴하여 가지치기를 했다. 최종적으로 가능한 경우면 현재까지 구한 최댓값과 비교 갱신해줬다.

```java
import java.io.*;
import java.util.*;

public class Main {

    private static int N;
    private static int M;
    private static int[] days;
    private static int[] pages;
    private static int max = 0;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        N = Integer.parseInt(st.nextToken());
        M = Integer.parseInt(st.nextToken());

        days = new int[M];
        pages = new int[M];
        for (int i = 0; i < M; i++) {
            st = new StringTokenizer(br.readLine());
            days[i] = Integer.parseInt(st.nextToken());
            pages[i] = Integer.parseInt(st.nextToken());
        }

        powerSet(0, 0, 0);

        System.out.println(max);
    }

    private static void powerSet(int idx, int daySum, int pageSum) {
        if (daySum > N) return;  // 읽는데 소요된 일수의 합이 N보다 크면 불가능한 경우

        if (idx == M) {
            max = Math.max(max, pageSum);
            return;
        }

        powerSet(idx + 1, daySum + days[idx], pageSum + pages[idx]);  // 현재 챕터를 읽는 경우
        powerSet(idx + 1, daySum, pageSum);  // 현재 챕터를 안 읽는 경우
    }
}
```

### 2. Bottom-Up 2차원 dp [Java]

기본적인 0-1 배낭 문제로 탐색했다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int M = Integer.parseInt(st.nextToken());

        int[] days = new int[1 + M];
        int[] pages = new int[1 + M];
        for (int i = 1; i <= M; i++) {
            st = new StringTokenizer(br.readLine());
            days[i] = Integer.parseInt(st.nextToken());
            pages[i] = Integer.parseInt(st.nextToken());
        }

        int[][] dp = new int[1 + M][1 + N];
        for (int i = 1; i <= M; i++) {
            int day = days[i];
            int page = pages[i];

            for (int j = 1; j <= N; j++) {
                if (j < day) {
                    dp[i][j] = dp[i - 1][j];
                } else {
                    dp[i][j] = Math.max(dp[i - 1][j - day] + page, dp[i - 1][j]);
                }
            }
        }

        System.out.println(dp[M][N]);
    }
}
```

### 3. Bottom-Up 1차원 dp [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int M = Integer.parseInt(st.nextToken());

        int[] days = new int[M];
        int[] pages = new int[M];
        for (int i = 0; i < M; i++) {
            st = new StringTokenizer(br.readLine());
            days[i] = Integer.parseInt(st.nextToken());
            pages[i] = Integer.parseInt(st.nextToken());
        }

        int[] dp = new int[1 + N];
        for (int i = 0; i < M; i++) {
            int day = days[i];
            int page = pages[i];

            for (int j = N; j >= day; j--) {
                dp[j] = Math.max(dp[j - day] + page, dp[j]);
            }
        }

        System.out.println(dp[N]);
    }
}
```

---

## 풀이 정보

### 1. 브루트 포스 [Java]

- 12 min

![](/assets/posts/2025-11/백준%2016493%20최대%20페이지%20수/photo3.png)

### 2. Bottom-Up 2차원 dp [Java]

- 6 min

![](/assets/posts/2025-11/백준%2016493%20최대%20페이지%20수/photo4.png)

### 3. Bottom-Up 1차원 dp [Java]

- 2 min

![](/assets/posts/2025-11/백준%2016493%20최대%20페이지%20수/photo5.png)

---

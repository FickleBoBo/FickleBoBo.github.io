---
title: "[Programmers] #181912 - 배열 만들기 5 [Java][C++][Python]"
date: 2026-08-25
categories: [PS, Programmers]
tags: ["string"]
slug: programmers-181912
media_subpath: /assets/img/posts/programmers-181912/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181912)
{: .prompt-info }

---

## 1. 아이디어

문자열 배열 `intStrs`의 각 원소에서 인덱스 `s`부터 길이 `l`만큼의 부분 문자열을 잘라 정수로 변환하고, 그 값이 `k`보다 큰 경우만 배열에 담아서 반환하면 된다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `intStrs`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.util.*;

class Solution {
    public int[] solution(String[] intStrs, int k, int s, int l) {
        List<Integer> list = new ArrayList<>();
        for (String str : intStrs) {
            int x = Integer.parseInt(str.substring(s, s + l));
            if (x > k) list.add(x);
        }

        return list.stream().mapToInt(Integer::intValue).toArray();
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

vector<int> solution(vector<string> intStrs, int k, int s, int l) {
    vector<int> v;
    for (auto& str : intStrs) {
        int x = stoi(str.substr(s, l));
        if (x > k) v.push_back(x);
    }

    return v;
}
```

```python
def solution(intStrs, k, s, l):
    res = []
    for token in intStrs:
        x = int(token[s : s + l])
        if x > k:
            res.append(x)

    return res
```

---

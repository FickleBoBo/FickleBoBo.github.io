---
title: "[Programmers] #181913 - 문자열 여러 번 뒤집기 [Java][C++][Python]"
date: 2026-08-25
categories: [PS, Programmers]
tags: ["implementation"]
description: "여러 구간 뒤집기 쿼리를 순서대로 적용해 최종 문자열을 구하는 문제."
slug: programmers-181913
media_subpath: /assets/img/posts/programmers-181913/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181913)
{: .prompt-info }

---

## 1. 아이디어

`queries`에 담긴 `[s, e]` 구간들을 순서대로 하나씩 뒤집어 나가면 되는 문제다. 각 쿼리마다 `s`와 `e`를 양 끝에서 좁혀오는 투 포인터로 두 문자를 맞바꾸면 해당 구간이 뒤집히고, 이를 쿼리 개수만큼 반복하면 최종 문자열이 완성된다.

---

## 2. 복잡도

| 접근 | 시간            | 공간   |
| ---- | --------------- | ------ |
| 풀이 | $O(N \times Q)$ | $O(N)$ |

($N$ = `my_string`의 길이, $Q$ = `queries`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public String solution(String my_string, int[][] queries) {
        char[] arr = my_string.toCharArray();
        for (int[] q : queries) {
            reverse(arr, q[0], q[1]);
        }

        return new String(arr);
    }

    static void reverse(char[] arr, int s, int e) {
        while (s < e) {
            char tmp = arr[s];
            arr[s] = arr[e];
            arr[e] = tmp;
            s++;
            e--;
        }
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

string solution(string my_string, vector<vector<int>> queries) {
    for (auto& q : queries) {
        reverse(my_string.begin() + q[0], my_string.begin() + q[1] + 1);
    }

    return my_string;
}
```

```python
def solution(my_string, queries):
    s = list(my_string)
    for a, b in queries:
        s[a : b + 1] = s[a : b + 1][::-1]

    return "".join(s)
```

---

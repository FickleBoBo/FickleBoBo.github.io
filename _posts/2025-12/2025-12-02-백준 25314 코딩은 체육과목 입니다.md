---
title: "[백준] 25314번 - 코딩은 체육과목 입니다 [Java][C++]"
date: 2025-12-02
categories: [PS, BaekJoon]
tags: [구현]
toc: true
math: true
image: /assets/posts/2025-12/백준%2025314%20코딩은%20체육과목%20입니다/thumbnail.png
---

[문제 링크](https://www.acmicpc.net/problem/25314)

---

## 1. 문제 풀이

$4$ 바이트이면 `long int`, $12$ 바이트면 `long long long int` 와 같은 형식으로 출력하는 문제로 $N$ 바이트의 경우 `long` 의 출력 횟수가 $N / 4$ 가 됨을 알 수 있다.

---

## 2. 코드

### 1. 구현 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();

        int N = Integer.parseInt(br.readLine());

        for (int i = 0; i < N / 4; i++) {
            sb.append("long ");
        }
        sb.append("int");

        bw.write(sb.toString());
        bw.flush();
    }
}
```

### 2. 구현 [Java]

`String` 의 `repeat` 메서드를 사용하면 좀 더 간단하게 작성할 수 있다.

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();

        int N = Integer.parseInt(br.readLine());

        sb.append("long ".repeat(N / 4)).append("int");

        bw.write(sb.toString());
        bw.flush();
    }
}
```

### 3. 구현 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin >> N;

    for (int i = 0; i < N / 4; i++) {
        cout << "long ";
    }
    cout << "int";
}
```

---

## 3. 풀이 정보

### 1. 구현 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 108 ms | 14212 KB | 540 B     |

### 2. 구현 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 100 ms | 14188 KB | 486 B     |

### 3. 구현 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2020 KB | 232 B     |

---

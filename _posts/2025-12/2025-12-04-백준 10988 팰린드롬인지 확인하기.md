---
title: "[백준] 10988번 - 팰린드롬인지 확인하기 [Java][C++]"
date: 2025-12-04
categories: [PS, BaekJoon]
tags: [구현, 문자열]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/10988)

---

## 1. 문제 풀이

주어진 문자열의 양 끝에 포인터를 위치시킨 후 한 칸씩 이동하며 서로 가리키는 문자가 일치하는지 비교하는 방식으로 해결해도 되고, 주어진 문자열을 뒤집은 후 원본 문자열과 일치하는지 비교하는 방식으로 해결해도 된다.

---

## 2. 코드

### 1. 구현 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        String str = br.readLine();
        String revStr = new StringBuilder(str).reverse().toString();

        if (str.equals(revStr)) {
            System.out.println(1);
        } else {
            System.out.println(0);
        }
    }
}
```

### 2. 구현 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;
    cin >> s;

    string rs = string(s.rbegin(), s.rend());

    if (s == rs) {
        cout << 1;
    } else {
        cout << 0;
    }
}
```

---

## 3. 풀이 정보

### 1. 구현 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 108 ms | 14212 KB | 431 B     |

### 2. 구현 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2024 KB | 270 B     |

---

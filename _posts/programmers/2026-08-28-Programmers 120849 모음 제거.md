---
title: "[Programmers] #120849 - 모음 제거 [Java][C++][Python]"
date: 2026-08-28
categories: [PS, Programmers]
tags: ["warm up"]
description: "소문자와 공백으로 이루어진 문자열에서 모음 a, e, i, o, u를 모두 제거한 문자열을 구하는 워밍업 문제."
slug: programmers-120849
media_subpath: /assets/img/posts/programmers-120849/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/120849)
{: .prompt-info }

---

## 1. 아이디어

문자열을 앞에서부터 한 글자씩 훑어 모음 `a`, `e`, `i`, `o`, `u`가 아닌 글자만 순서대로 이어 붙이면 된다. 공백은 모음이 아니므로 그대로 남는다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `my_string`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public String solution(String my_string) {
        StringBuilder sb = new StringBuilder();
        for (char c : my_string.toCharArray()) {
            if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') continue;
            sb.append(c);
        }

        return sb.toString();
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

string solution(string my_string) {
    string s;
    for (char c : my_string) {
        if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') continue;
        s += c;
    }

    return s;
}
```

```python
def solution(my_string):
    return "".join(c for c in my_string if c not in "aeiou")
```

---

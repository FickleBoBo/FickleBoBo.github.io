---
title: "[백준] 13711번 - LCS 4 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [다이나믹 프로그래밍, LCS, LIS, 이분 탐색]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/13711)

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo1.png)
![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo2.png)

---

## 문제 풀이

`1` 부터 `N`까지 한 번씩 등장하는 두 수열 `A`, `B`의 LCS를 구하는 문제로 `N`이 최대 `100,000`이어서 일반적인 $O(N^2)$의 시간복잡도를 갖는 LCS 알고리즘으로는 해결할 수 없다. 해당 문제는 각 자연수가 한 번씩만 등장한다는 점에서 LIS를 활용하면 해결할 수 있다.

포인트는 `A`의 각 원소가 `B`에서 등장한 위치에 대한 LIS를 구하는 것으로 LIS가 `A`의 인덱스가 커지는 방향으로 진행되면서 LIS를 계산할 배열은 `B`의 위치에 대한 LIS이므로 서로 증가하는 방향으로 구할 수 있어 LCS를 구할 수 있다.

예를 들면 $A = {3, 1, 2, 4, 5}$, $B = {3, 2, 4, 5, 1}$ 일 때,

$A$

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo01.drawio.svg)

$B$

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo02.drawio.svg)

LCS는 아래와 같다.

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo03.drawio.svg)

$A$는 아래와 같이 그냥 배열로 받고,

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo04.drawio.svg)

$B$는 아래와 같이 2차원 배열로 받는데 이때 값과 인덱스(위치)를 같이 받는다.

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo05.drawio.svg)

이후 $B$는 값에 대한 오름차순으로 정렬한다.($B$에서 바로 찾기 위해 정렬)

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo07.drawio.svg)

이후 $A$의 각 원소에 대해 $B$에서 등장한 위치를 배열로 받으면 아래와 같이 받을 수 있다.

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo08.drawio.svg)

해당 배열의 LIS는 아래와 같고 $A$의 LIS 위치와 일치하는 것을 볼 수 있다.

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo09.drawio.svg)

LIS 역시 이분 탐색 LIS를 적용해야 시간 내에 해결할 수 있다.

---

## 코드

### 1. 이분 탐색 LIS [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[] A = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            A[i] = Integer.parseInt(st.nextToken());
        }

        int[][] B = new int[N][2];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            B[i][0] = Integer.parseInt(st.nextToken());
            B[i][1] = i;
        }
        Arrays.sort(B, ((o1, o2) -> Integer.compare(o1[0], o2[0])));

        int[] arr = new int[N];
        for (int i = 0; i < N; i++) {
            arr[i] = B[A[i] - 1][1];
        }

        int[] dp = new int[1 + N];
        int len = 0;

        for (int i = 0; i < N; i++) {
            int idx = lowerBound(dp, len, arr[i]);

            dp[idx] = arr[i];
            if (idx == len + 1) len++;
        }

        System.out.println(len);
    }

    private static int lowerBound(int[] arr, int len, int key) {
        int left = 1;
        int right = len + 1;

        while (left < right) {
            int mid = (left + right) / 2;

            if (arr[mid] < key) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }

        return right;
    }
}
```

---

## 풀이 정보

### 1. 이분 탐색 LIS [Java]

- 46 min

![](/assets/posts/2025-11/백준%2013711%20LCS%204/photo3.png)

---

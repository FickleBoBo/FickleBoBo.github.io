---
title: "[Programmers] #120823 - 직각삼각형 출력하기 [Java][C++][Python]"
date: 2026-08-24
categories: [PS, Programmers]
tags: ["warm up"]
slug: programmers-120823
media_subpath: /assets/img/posts/programmers-120823/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/120823)
{: .prompt-info }

---

## 1. 아이디어

1부터 `n`까지 순회하면서 `i`번째 줄에 별을 `i`개씩 출력하면 높이와 너비가 `n`인 직각삼각형을 출력할 수 있다.

---

## 2. 복잡도

| 접근 | 시간     | 공간   |
| ---- | -------- | ------ |
| 풀이 | $O(N^2)$ | $O(N)$ |

($N$ = 입력값 `n`. Java는 전체 출력을 `sb`에 모았다가 출력하므로 공간 $O(N^2)$)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.io.*;

public class Solution {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        int n = Integer.parseInt(br.readLine());
        for (int i = 1; i <= n; i++) {
            sb.repeat("*", i).append("\n");
        }

        System.out.println(sb);
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    for (int i = 1; i <= n; i++) {
        cout << string(i, '*') << '\n';
    }
}
```

```python
import sys

input = sys.stdin.readline

n = int(input())
for i in range(1, n + 1):
    print("*" * i)
```

---

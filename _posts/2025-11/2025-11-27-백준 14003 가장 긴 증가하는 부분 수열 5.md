---
title: "[백준] 14003번 - 가장 긴 증가하는 부분 수열 5 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [다이나믹 프로그래밍, LIS, 이분 탐색, 역추적]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/14003)

![](/assets/posts/2025-11/백준%2014003%20가장%20긴%20증가하는%20부분%20수열%205/photo1.png)
![](/assets/posts/2025-11/백준%2014003%20가장%20긴%20증가하는%20부분%20수열%205/photo2.png)

---

## 문제 풀이

[백준 12738번 - 가장 긴 증가하는 부분 수열 3]({% post_url 2025-11/2025-11-27-백준 12738 가장 긴 증가하는 부분 수열 3 %}) 문제에서 실제 LIS까지 출력해야 하는 문제다. 이분 탐색 LIS의 dp 배열은 다음 원소가 계속 덮어쓰는 구조라 dp 배열만으로는 실제 LIS를 알아낼 수 없는데 이를 위해 임시 배열을 만들고 해당 원소를 포함하는 LIS의 길이를 저장하여 이를 역순 조회로 실제 LIS를 구할 수 있게 해줘야 한다.

---

## 코드

### 1. 이분 탐색 LIS [Java]

역순으로 조회하므로 `Deque`를 스택으로 활용하여 정순으로 출력하게 해줬다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[] arr = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        int[] dp = new int[1 + N];
        dp[0] = Integer.MIN_VALUE;
        int len = 0;

        int[] prev = new int[N];

        for (int i = 0; i < N; i++) {
            int idx = lowerBound(dp, len, arr[i]);

            dp[idx] = arr[i];
            if (idx == len + 1) len++;

            prev[i] = idx;
        }

        sb.append(len).append('\n');

        Deque<Integer> stack = new ArrayDeque<>();
        int tmp = len;
        for (int i = N - 1; i >= 0; i--) {
            if (prev[i] == tmp) {
                stack.push(arr[i]);
                tmp--;
            }
        }

        while (!stack.isEmpty()) {
            sb.append(stack.pop()).append(" ");
        }

        bw.write(sb.toString());
        bw.flush();
    }

    private static int lowerBound(int[] arr, int len, int key) {
        int left = 1;
        int right = len + 1;

        while (left < right) {
            int mid = (left + right) / 2;

            if (arr[mid] < key) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }

        return right;
    }
}
```

---

## 풀이 정보

### 1. 이분 탐색 LIS [Java]

- 42 min

![](/assets/posts/2025-11/백준%2014003%20가장%20긴%20증가하는%20부분%20수열%205/photo3.png)

---

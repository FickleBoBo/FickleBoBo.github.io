---
title: "[백준] 2581번 - 소수 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [수학, 정수론, 소수 판정, 에라토스테네스의 체]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/2581)

![](/assets/posts/2025-11/백준%202581%20소수/photo1.png)
![](/assets/posts/2025-11/백준%202581%20소수/photo2.png)

---

## 문제 풀이

`M` 이상 `N` 이하인 소수의 합과 최솟값을 출력하는 문제로 빠르게 판정할 수 있는 에라토스테네스의 체를 활용하면 간단하게 해결할 수 있다.

---

## 코드

### 1. 에라토스테네스의 체 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int M = Integer.parseInt(br.readLine());
        int N = Integer.parseInt(br.readLine());

        boolean[] isPrime = sieve(N);

        int sum = 0;
        int min = Integer.MAX_VALUE;
        for (int i = M; i <= N; i++) {
            if (isPrime[i]) {
                sum += i;
                min = Math.min(min, i);
            }
        }

        if (sum == 0) {
            System.out.println(-1);
        } else {
            System.out.println(sum);
            System.out.println(min);
        }
    }

    // 에라토스테네스의 체
    private static boolean[] sieve(int N) {
        boolean[] isPrime = new boolean[1 + N];
        Arrays.fill(isPrime, 2, N + 1, true);

        for (int i = 2; i * i <= N; i++) {
            if (isPrime[i]) {
                for (int j = i * i; j <= N; j += i) {
                    isPrime[j] = false;
                }
            }
        }

        return isPrime;
    }
}
```

---

## 풀이 정보

### 1. 에라토스테네스의 체 [Java]

- 3 min

![](/assets/posts/2025-11/백준%202581%20소수/photo3.png)

---

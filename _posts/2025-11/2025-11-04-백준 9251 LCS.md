---
title: "[백준] 9251번 - LCS [Java]"
date: 2025-11-04
categories: [PS, BaekJoon]
tags: [문자열, 다이나믹 프로그래밍, LCS]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/9251)

![](/assets/posts/2025-11/백준%209251%20LCS/photo1.png)
![](/assets/posts/2025-11/백준%209251%20LCS/photo2.png)

---

## 문제 풀이

전형적인 [LCS]({% post_url 2025-11/2025-11-16-LCS (Longest Common Subsequence) %}) 문제로 다이나믹 프로그래밍을 통해 해결할 수 있다. Bottom-Up 방식과 Top-Down 방식 두 가지로 해결했다.

---

## 코드

### 1. Bottom-Up dp [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] str1 = br.readLine().toCharArray();
        char[] str2 = br.readLine().toCharArray();
        int N = str1.length;
        int M = str2.length;

        int[][] dp = new int[1 + N][1 + M];
        for (int i = 1; i <= N; i++) {
            for (int j = 1; j <= M; j++) {
                if (str1[i - 1] == str2[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                } else {
                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }

        System.out.println(dp[N][M]);
    }
}
```

### 2. Top-Down dp [Java]

Top-Down 방식의 경우 일반적인 재귀만으로는 해결할 수 없어서 메모이제이션을 활용해야 하는데 이때의 메모이제이션 테이블이 dp 테이블과 동일한 모양이 된다. 갱신 여부는 방문 체크 배열을 별도로 만들어서 방문 체크와 메모 및 메모값 반환으로 구성했다.

```java
import java.io.*;

public class Main {

    private static int[][] dp;
    private static boolean[][] visited;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] str1 = br.readLine().toCharArray();
        char[] str2 = br.readLine().toCharArray();
        int N = str1.length;
        int M = str2.length;

        dp = new int[N][M];
        visited = new boolean[N][M];

        System.out.println(lcs(str1, N - 1, str2, M - 1));
    }

    private static int lcs(char[] str1, int len1, char[] str2, int len2) {
        if (len1 == -1 || len2 == -1) return 0;

        if (visited[len1][len2]) return dp[len1][len2];

        visited[len1][len2] = true;
        if (str1[len1] == str2[len2]) {
            return dp[len1][len2] = lcs(str1, len1 - 1, str2, len2 - 1) + 1;
        } else {
            return dp[len1][len2] = Math.max(lcs(str1, len1 - 1, str2, len2), lcs(str1, len1, str2, len2 - 1));
        }
    }
}
```

---

## 풀이 정보

### 1. Bottom-Up dp [Java]

- 25 min

![](/assets/posts/2025-11/백준%209251%20LCS/photo3.png)

### 2. Top-Down dp [Java]

- 16 min

![](/assets/posts/2025-11/백준%209251%20LCS/photo4.png)

---

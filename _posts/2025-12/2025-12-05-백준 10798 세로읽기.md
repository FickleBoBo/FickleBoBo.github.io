---
title: "[백준] 10798번 - 세로읽기 [Java][C++]"
date: 2025-12-05
categories: [PS, BaekJoon]
tags: [구현, 문자열]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/10798)

---

## 1. 문제 풀이

주어진 글자들을 세로로 읽는 문제로 2차원 배열을 활용하여 입력을 받고 이를 열 우선 순회로 탐색하면 된다.

---

## 2. 코드

### 1. 구현 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();

        char[][] grid = new char[5][15];
        for (int i = 0; i < 5; i++) {
            String input = br.readLine();

            for (int j = 0; j < input.length(); j++) {
                grid[i][j] = input.charAt(j);
            }
        }

        for (int j = 0; j < 15; j++) {
            for (int i = 0; i < 5; i++) {
                if (grid[i][j] != '\u0000') sb.append(grid[i][j]);
            }
        }

        bw.write(sb.toString());
        bw.flush();
    }
}
```

### 2. 구현 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    char grid[5][15] = {};
    for (int i = 0; i < 5; i++) {
        string s;
        cin >> s;

        for (int j = 0; j < s.size(); j++) {
            grid[i][j] = s[j];
        }
    }

    for (int j = 0; j < 15; j++) {
        for (int i = 0; i < 5; i++) {
            if (grid[i][j] != '\0') cout << grid[i][j];
        }
    }
}
```

---

## 3. 풀이 정보

### 1. 구현 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 100 ms | 14244 KB | 800 B     |

### 2. 구현 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2024 KB | 454 B     |

---

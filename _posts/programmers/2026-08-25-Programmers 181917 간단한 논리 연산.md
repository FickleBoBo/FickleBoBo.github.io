---
title: "[Programmers] #181917 - 간단한 논리 연산 [Java][C++][Python]"
date: 2026-08-25
categories: [PS, Programmers]
tags: ["warm up"]
slug: programmers-181917
media_subpath: /assets/img/posts/programmers-181917/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181917)
{: .prompt-info }

---

## 1. 아이디어

$(x_1 \lor x_2) \land (x_3 \lor x_4)$ 식을 언어의 논리 연산자로 그대로 옮기면 되는 문제다. `x1`, `x2` 중 하나라도 참이면 앞쪽 조건이 참이 되고, `x3`, `x4` 중 하나라도 참이면 뒤쪽 조건이 참이 되며, 두 조건이 모두 참일 때만 전체 결과가 참이 된다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(1)$ | $O(1)$ |

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public boolean solution(boolean x1, boolean x2, boolean x3, boolean x4) {
        return (x1 || x2) && (x3 || x4);
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

bool solution(bool x1, bool x2, bool x3, bool x4) {
    return (x1 || x2) && (x3 || x4);
}
```

```python
def solution(x1, x2, x3, x4):
    return (x1 or x2) and (x3 or x4)
```

---

---
title: "[백준] 1629번 - 곱셈 [Java]"
date: 2025-11-06
categories: [PS, BaekJoon]
tags: [수학, 분할 정복, 분할 정복을 이용한 거듭제곱]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/1629)

![](/assets/posts/2025-11/백준%201629%20곱셈/photo1.png)
![](/assets/posts/2025-11/백준%201629%20곱셈/photo2.png)

---

## 문제 풀이

간단하게 $A^B \ (\mathrm{mod}\ C)$를 구해야 하는 문제다. `B`의 크기가 매우 커질 수 있어서 반복적으로 곱하는 방식으로는 해결할 수 없는데 분할 정복을 이용한 거듭제곱이라는 테크닉을 활용하면 해결할 수 있다.

---

## 코드

### 1. 분할 정복을 이용한 거듭제곱 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int A = Integer.parseInt(st.nextToken());
        int B = Integer.parseInt(st.nextToken());
        int C = Integer.parseInt(st.nextToken());

        System.out.println(modPow(A, B, C));
    }

    public static long modPow(long A, long B, long C) {
        if (B == 0) return 1;

        long half = modPow(A, B / 2, C);

        if (B % 2 == 0) {
            return half * half % C;
        } else {
            return half * half % C * A % C;  // half * half * A가 long형 범위를 넘어갈 수 있어서 미리 mod 수행
        }
    }
}
```

---

## 풀이 정보

### 1. 분할 정복을 이용한 거듭제곱 [Java]

- 3 min

![](/assets/posts/2025-11/백준%201629%20곱셈/photo3.png)

---

---
title: "[Programmers] #120842 - 2차원으로 만들기 [Java][C++][Python]"
date: 2026-08-26
categories: [PS, Programmers]
tags: ["warm up"]
description: "정수 배열을 앞에서부터 n개씩 끊어 2차원 배열로 만드는 워밍업 문제."
slug: programmers-120842
media_subpath: /assets/img/posts/programmers-120842/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/120842)
{: .prompt-info }

---

## 1. 아이디어

`num_list`의 길이가 `n`의 배수이므로, 앞에서부터 `n`개씩 잘라 행으로 쌓으면 빈 자리 없이 맞아떨어진다.

행의 개수는 `num_list`의 길이를 `n`으로 나눈 값이고, `i`번째 행은 원본의 `i * n`부터 `(i + 1) * n` 직전까지를 그대로 옮긴 것이다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `num_list`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public int[][] solution(int[] num_list, int n) {
        int[][] arr = new int[num_list.length / n][n];
        for (int i = 0; i < num_list.length / n; i++) {
            System.arraycopy(num_list, i * n, arr[i], 0, n);
        }

        return arr;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> solution(vector<int> num_list, int n) {
    vector<vector<int>> v(num_list.size() / n, vector<int>(n));
    for (int i = 0; i < num_list.size() / n; i++) {
        copy(num_list.begin() + i * n, num_list.begin() + (i + 1) * n, v[i].begin());
    }

    return v;
}
```

```python
def solution(num_list, n):
    return [num_list[i : i + n] for i in range(0, len(num_list), n)]
```

---

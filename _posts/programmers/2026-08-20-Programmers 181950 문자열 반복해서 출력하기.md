---
title: "[Programmers] #181950 - 문자열 반복해서 출력하기 [Java][C++][Python]"
date: 2026-08-20
categories: [PS, Programmers]
tags: ["warm up"]
description: "문자열 str을 n번 반복해 출력하는 워밍업 문제."
slug: programmers-181950
media_subpath: /assets/img/posts/programmers-181950/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181950)
{: .prompt-info }

---

## 1. 아이디어

문자열 `str`과 반복 횟수 `n`을 입력받아 `str`을 `n`번 이어붙여 출력하면 되는 문제다.

---

## 2. 복잡도

| 접근 | 시간            | 공간            |
| ---- | --------------- | --------------- |
| 풀이 | $O(N \times K)$ | $O(N \times K)$ |

($N$ = `str`의 길이, $K$ = 반복 횟수 `n`)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.io.*;
import java.util.*;

public class Solution {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        String s = st.nextToken();
        int n = Integer.parseInt(st.nextToken());
        System.out.println(s.repeat(n));
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s;
    int n;
    cin >> s >> n;

    while (n--) cout << s;
}
```

C++ 표준 라이브러리엔 Java의 `repeat`, Python의 `*`에 대응하는 문자열 반복 연산이 없어 `n`번 반복문을 돌며 출력해줬다.

```python
import sys

input = sys.stdin.readline

s, n = input().split()
print(s * int(n))
```

---

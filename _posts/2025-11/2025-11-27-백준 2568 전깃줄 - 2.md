---
title: "[백준] 2568번 - 전깃줄 - 2 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [다이나믹 프로그래밍, LIS, 이분 탐색, 역추적]
toc: true
math: true
image: /assets/posts/2025-11/백준%202568%20전깃줄%20-%202/thumbnail.avif
---

[문제 링크](https://www.acmicpc.net/problem/2568)

![](/assets/posts/2025-11/백준%202568%20전깃줄%20-%202/photo1.png)
![](/assets/posts/2025-11/백준%202568%20전깃줄%20-%202/photo2.png)

---

## 문제 풀이

[백준 2565번 - 전깃줄]({% post_url 2025-11/2025-11-27-백준 2565 전깃줄 %}) 해당 문제에서 전깃줄의 개수가 늘어나고 없애야 할 전깃줄도 출력해야 하는 문제로 기존 문제의 로직에서 이분 탐색 LIS와 역추적을 활용하면 된다.

---

## 코드

### 1. 이분 탐색 LIS [Java]

역추적 과정에서 LIS가 아닌 전깃줄을 포함해야 한다는 점과 `A`의 위치를 포함해야 함에 주의해야 한다. 역방향으로 탐색하면 `A`의 위치 번호도 역순으로 조회되므로 스택 자료구조를 활용하면 오름차순으로 출력할 수 있다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());

        int[][] map = new int[N][2];
        for (int i = 0; i < N; i++) {
            st = new StringTokenizer(br.readLine());
            map[i][0] = Integer.parseInt(st.nextToken());
            map[i][1] = Integer.parseInt(st.nextToken());
        }
        Arrays.sort(map, (o1, o2) -> Integer.compare(o1[0], o2[0]));

        int[] dp = new int[1 + N];
        int len = 0;

        int[] before = new int[N];

        for (int i = 0; i < N; i++) {
            int idx = lowerBound(dp, len, map[i][1]);

            dp[idx] = map[i][1];
            if (idx == len + 1) len++;

            before[i] = idx;
        }

        sb.append(N - len).append('\n');

        Deque<Integer> stack = new ArrayDeque<>();
        int tmp = len;
        for (int i = N - 1; i >= 0; i--) {
            if (before[i] == tmp) {
                tmp--;
            } else {
                stack.push(map[i][0]);
            }
        }

        while (!stack.isEmpty()) {
            sb.append(stack.pop()).append('\n');
        }

        bw.write(sb.toString());
        bw.flush();
    }

    private static int lowerBound(int[] arr, int len, int key) {
        int left = 1;
        int right = len + 1;

        while (left < right) {
            int mid = (left + right) / 2;

            if (arr[mid] < key) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }

        return right;
    }
}
```

---

## 풀이 정보

### 1. 이분 탐색 LIS [Java]

- 8 min

![](/assets/posts/2025-11/백준%202568%20전깃줄%20-%202/photo3.png)

---

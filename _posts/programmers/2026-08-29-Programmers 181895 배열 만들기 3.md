---
title: "[Programmers] #181895 - 배열 만들기 3 [Java][C++][Python]"
date: 2026-08-29
categories: [PS, Programmers]
tags: ["warm up"]
slug: programmers-181895
media_subpath: /assets/img/posts/programmers-181895/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181895)
{: .prompt-info }

---

## 1. 아이디어

`intervals`의 각 구간 `[s, e]`에 대해 `arr`의 `s`번째부터 `e`번째까지 원소를 순서대로 모아 이어 붙이면 된다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `arr`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.util.*;

class Solution {
    public int[] solution(int[] arr, int[][] intervals) {
        List<Integer> list = new ArrayList<>();
        for (int[] interval : intervals) {
            for (int i = interval[0]; i <= interval[1]; i++) {
                list.add(arr[i]);
            }
        }

        return list.stream().mapToInt(Integer::intValue).toArray();
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

vector<int> solution(vector<int> arr, vector<vector<int>> intervals) {
    vector<int> v;
    for (auto& interval : intervals) {
        for (int i = interval[0]; i <= interval[1]; i++) {
            v.push_back(arr[i]);
        }
    }

    return v;
}
```

```python
def solution(arr, intervals):
    (s1, e1), (s2, e2) = intervals
    return arr[s1 : e1 + 1] + arr[s2 : e2 + 1]
```

구간이 2개이므로 튜플들로 깔끔하게 받았다.

---

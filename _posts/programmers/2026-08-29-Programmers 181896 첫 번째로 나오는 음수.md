---
title: "[Programmers] #181896 - 첫 번째로 나오는 음수 [Java][C++][Python]"
date: 2026-08-29
categories: [PS, Programmers]
tags: ["warm up"]
description: "정수 리스트에서 처음 나오는 음수의 인덱스를, 음수가 없으면 -1을 반환하는 워밍업 문제."
slug: programmers-181896
media_subpath: /assets/img/posts/programmers-181896/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181896)
{: .prompt-info }

---

## 1. 아이디어

`num_list`를 앞에서부터 순회하며 처음 만나는 음수의 인덱스를 반환하고, 끝까지 음수가 없으면 -1을 반환한다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(1)$ |

($N$ = `num_list`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public int solution(int[] num_list) {
        for (int i = 0; i < num_list.length; i++) {
            if (num_list[i] < 0) return i;
        }

        return -1;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int solution(vector<int> num_list) {
    for (int i = 0; i < num_list.size(); i++) {
        if (num_list[i] < 0) return i;
    }

    return -1;
}
```

```python
def solution(num_list):
    return next((i for i, x in enumerate(num_list) if x < 0), -1)
```

---

---
title: "[Programmers] #120818 - 옷가게 할인 받기 [Java][C++][Python]"
date: 2026-08-23
categories: [PS, Programmers]
tags: ["warm up"]
slug: programmers-120818
media_subpath: /assets/img/posts/programmers-120818/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/120818)
{: .prompt-info }

---

## 1. 아이디어

구매한 옷의 가격 `price`가 50만 원, 30만 원, 10만 원 구간 중 어디에 속하는지에 따라 할인율(20%, 10%, 5%)이 정해지고, 어느 구간에도 속하지 않으면 할인이 없다. 구간이 겹치므로 큰 금액부터 순서대로 조건문으로 확인하여 `price`에 해당하는 할인율을 적용하면 해결할 수 있다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(1)$ | $O(1)$ |

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public int solution(int price) {
        if (price >= 500000) return price * 80 / 100;
        if (price >= 300000) return price * 90 / 100;
        if (price >= 100000) return price * 95 / 100;
        return price;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int solution(int price) {
    if (price >= 500000) return price * 80 / 100;
    if (price >= 300000) return price * 90 / 100;
    if (price >= 100000) return price * 95 / 100;
    return price;
}
```

```python
def solution(price):
    if price >= 500000:
        return price * 80 // 100
    if price >= 300000:
        return price * 90 // 100
    if price >= 100000:
        return price * 95 // 100
    return price
```

---

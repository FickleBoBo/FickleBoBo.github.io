---
title: "[백준] 11660번 - 구간 합 구하기 5 [Java]"
date: 2025-11-22
categories: [PS, BaekJoon]
tags: [누적합]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/11660)

![](/assets/posts/2025-11/백준%2011660%20구간%20합%20구하기%205/photo1.png)
![](/assets/posts/2025-11/백준%2011660%20구간%20합%20구하기%205/photo2.png)

---

## 문제 풀이

$(x1, y1)$ 부터 $(x2, y2)$ 까지 영역의 합을 `M`번 구해야 하는 문제로 매번 반복해서 찾으면 시간이 오래 걸린다. 1차원 누적합을 응용한 2차원 누적합을 적용하면 해결할 수 있다.

---

## 코드

### 1. 누적합 [Java]

계산 편의를 위해 누적합 테이블은 왼쪽, 위쪽에 패딩을 한 칸 줬다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int M = Integer.parseInt(st.nextToken());

        int[][] map = new int[N][N];
        for (int i = 0; i < N; i++) {
            st = new StringTokenizer(br.readLine());

            for (int j = 0; j < N; j++) {
                map[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        int[][] prefixSum = new int[1 + N][1 + N];
        for (int i = 1; i <= N; i++) {
            for (int j = 1; j <= N; j++) {
                prefixSum[i][j] = prefixSum[i - 1][j] + prefixSum[i][j - 1] - prefixSum[i - 1][j - 1] + map[i - 1][j - 1];
            }
        }

        for (int i = 0; i < M; i++) {
            st = new StringTokenizer(br.readLine());
            int x1 = Integer.parseInt(st.nextToken());
            int y1 = Integer.parseInt(st.nextToken());
            int x2 = Integer.parseInt(st.nextToken());
            int y2 = Integer.parseInt(st.nextToken());

            sb.append(prefixSum[x2][y2] - prefixSum[x2][y1 - 1] - prefixSum[x1 - 1][y2] + prefixSum[x1 - 1][y1 - 1]).append('\n');
        }

        bw.write(sb.toString());
        bw.flush();
    }
}
```

---

## 풀이 정보

### 1. 누적합 [Java]

- 8 min

![](/assets/posts/2025-11/백준%2011660%20구간%20합%20구하기%205/photo3.png)

---

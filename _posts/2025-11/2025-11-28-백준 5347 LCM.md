---
title: "[백준] 5347번 - LCM [Java]"
date: 2025-11-28
categories: [PS, BaekJoon]
tags: [수학, 정수론, 유클리드 호제법]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/5347)

![](/assets/posts/2025-11/백준%205347%20LCM/photo1.png)

---

## 문제 풀이

각 자연수의 최소 공배수를 출력하는 문제로 최대 공약수를 구할 수 있는 유클리드 호제법을 활용하면 간단하게 해결할 수 있다. 최소 공배수는 두 수의 곱을 최대 공약수로 나누면 된다.

---

## 코드

### 1. 유클리드 호제법 [Java]

최소 공배수가 int형 범위를 넘어갈 수 있어서 long 타입으로 캐스팅을 해줬다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st;

        int n = Integer.parseInt(br.readLine());
        for (int tc = 1; tc <= n; tc++) {
            st = new StringTokenizer(br.readLine());
            int a = Integer.parseInt(st.nextToken());
            int b = Integer.parseInt(st.nextToken());

            sb.append(lcm(a, b)).append('\n');
        }

        bw.write(sb.toString());
        bw.flush();
    }

    private static int gcd(int a, int b) {
        if (b == 0) return a;
        return gcd(b, a % b);
    }

    private static long lcm(int a, int b) {
        return (long) a * b / gcd(a, b);
    }
}
```

---

## 풀이 정보

### 1. 유클리드 호제법 [Java]

- 2 min

![](/assets/posts/2025-11/백준%205347%20LCM/photo2.png)

---

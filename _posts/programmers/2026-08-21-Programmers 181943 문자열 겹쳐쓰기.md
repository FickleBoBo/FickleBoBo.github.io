---
title: "[Programmers] #181943 - 문자열 겹쳐쓰기 [Java][C++][Python]"
date: 2026-08-21
categories: [PS, Programmers]
tags: ["string"]
description: "my_string의 지정된 구간을 overwrite_string으로 덮어써 반환하는 문제."
slug: programmers-181943
media_subpath: /assets/img/posts/programmers-181943/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181943)
{: .prompt-info }

---

## 1. 아이디어

`my_string`의 인덱스 `s`부터 `overwrite_string`의 길이만큼을 `overwrite_string`으로 바꿔 반환하면 되는 문제다. `s`와 `overwrite_string`의 길이를 더한 값이 `my_string`의 길이를 넘지 않는다는 제약이 보장되므로, `my_string`을 `s` 앞부분과 겹쳐쓰기가 끝나는 지점 뒷부분으로 나눠 그 사이에 `overwrite_string`을 끼워 넣으면 된다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `my_string`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public String solution(String my_string, String overwrite_string, int s) {
        return my_string.substring(0, s) +
                overwrite_string +
                my_string.substring(s + overwrite_string.length());
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

string solution(string my_string, string overwrite_string, int s) {
    return my_string.substr(0, s) + overwrite_string + my_string.substr(s + overwrite_string.size());
}
```

```python
def solution(my_string, overwrite_string, s):
    return my_string[:s] + overwrite_string + my_string[s + len(overwrite_string) :]
```

---

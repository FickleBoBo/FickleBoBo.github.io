---
title: "[Programmers] #120834 - 외계행성의 나이 [Java][C++][Python]"
date: 2026-08-25
categories: [PS, Programmers]
tags: ["string"]
description: "정수 나이를 외계행성 알파벳 표기법으로 변환하는 문제."
slug: programmers-120834
media_subpath: /assets/img/posts/programmers-120834/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/120834)
{: .prompt-info }

---

## 1. 아이디어

`age`를 10진법 자릿수 그대로 유지한 채, 각 자리 숫자(0~9)를 `a`부터 `j`까지의 알파벳 문자로 치환하면 되는 문제다. 자리 순서만 지키면 되므로, `10`으로 나눈 나머지를 낮은 자리부터 모아 마지막에 뒤집든 10진수 문자열을 앞에서부터 훑든 상관없다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(D)$ | $O(D)$ |

($D$ = `age`의 자릿수, $D = O(\log_{10} \text{age})$)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public String solution(int age) {
        StringBuilder sb = new StringBuilder();
        while (age > 0) {
            sb.append((char) (age % 10 + 'a'));
            age /= 10;
        }

        return sb.reverse().toString();
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

string solution(int age) {
    string s;
    while (age) {
        s += age % 10 + 'a';
        age /= 10;
    }
    reverse(s.begin(), s.end());

    return s;
}
```

```python
def solution(age):
    return "".join(chr(int(d) + ord("a")) for d in str(age))
```

---

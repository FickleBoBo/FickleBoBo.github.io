---
title: "[백준] 17087번 - 숨바꼭질 6 [Java]"
date: 2025-11-28
categories: [PS, BaekJoon]
tags: [수학, 정수론, 유클리드 호제법]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/17087)

![](/assets/posts/2025-11/백준%2017087%20숨바꼭질%206/photo1.png)
![](/assets/posts/2025-11/백준%2017087%20숨바꼭질%206/photo2.png)

---

## 문제 풀이

수빈이는 현재 위치에서 `D`만큼 이동할 수 있을 때, 모든 동생을 찾을 수 있는 `D`의 최댓값을 구하는 문제로 수빈이의 초기 위치와 동생들과의 위치의 차들에 대한 최대 공약수를 `D`로 설정하면 모든 동생을 찾을 수 있으면서 가능한 `D`가 최대가 된다.

---

## 코드

### 1. 유클리드 호제법 [Java]

항상 거리로 판단할 수 있게 절댓값 처리를 해줬다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int S = Integer.parseInt(st.nextToken());

        int[] arr = new int[N];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        int gcd = Math.abs(S - arr[0]);
        for (int i = 1; i < N; i++) {
            gcd = gcd(gcd, Math.abs(S - arr[i]));
        }

        System.out.println(gcd);
    }

    private static int gcd(int a, int b) {
        if (b == 0) return a;
        return gcd(b, a % b);
    }
}
```

---

## 풀이 정보

### 1. 유클리드 호제법 [Java]

- 4 min

![](/assets/posts/2025-11/백준%2017087%20숨바꼭질%206/photo3.png)

---

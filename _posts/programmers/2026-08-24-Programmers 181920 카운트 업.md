---
title: "[Programmers] #181920 - 카운트 업 [Java][C++][Python]"
date: 2026-08-24
categories: [PS, Programmers]
tags: ["warm up"]
description: "start_num부터 end_num까지 정수를 순서대로 나열하는 워밍업 문제."
slug: programmers-181920
media_subpath: /assets/img/posts/programmers-181920/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181920)
{: .prompt-info }

---

## 1. 아이디어

`start_num`부터 `end_num`까지 정수를 하나씩 순서대로 담아 배열로 반환하면 되는 문제다. 범위를 그대로 순회하는 반복문 하나로 충분하다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = 반환할 배열의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public int[] solution(int start_num, int end_num) {
        int[] arr = new int[end_num - start_num + 1];
        for (int i = 0; i < arr.length; i++) {
            arr[i] = start_num + i;
        }

        return arr;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

vector<int> solution(int start_num, int end_num) {
    vector<int> v;
    for (int i = start_num; i <= end_num; i++) {
        v.push_back(i);
    }

    return v;
}
```

```python
def solution(start_num, end_num):
    return list(range(start_num, end_num + 1))
```

---

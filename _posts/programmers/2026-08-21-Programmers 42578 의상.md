---
title: "[Programmers] #42578 - 의상 [Java][C++][Python]"
date: 2026-08-21
categories: [PS, Programmers]
tags: ["hash table", "data structure", "combinatorics", "math"]
description: "종류별 의상 개수로 조합 가능한 코디 수를 계산하는 문제."
slug: programmers-42578
media_subpath: /assets/img/posts/programmers-42578/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/42578)
{: .prompt-info }

---

## 1. 아이디어

의상 목록 `clothes`가 주어질 때, 종류별로 최대 1가지씩만 착용하면서 최소 한 개는 입어야 하는 조합의 수를 구하면 되는 문제다. 먼저 해시맵으로 종류별 의상 개수를 셌다. 각 종류마다 "그 종류를 안 입기"와 "그 종류의 의상 중 하나를 입기" 중 하나를 고를 수 있으므로, 종류별 경우의 수는 (해당 종류 의상 개수 + 1)가지다. 이를 모든 종류에 대해 곱하면 아무것도 안 입는 경우까지 포함한 전체 조합 수가 나오므로, 마지막에 1을 빼서 최소 한 개는 입는 경우만 남겼다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(N)$ |

($N$ = `clothes`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.util.*;

class Solution {
    public int solution(String[][] clothes) {
        Map<String, Integer> cnt = new HashMap<>();
        for (String[] c : clothes) {
            cnt.put(c[1], cnt.getOrDefault(c[1], 0) + 1);
        }

        int ans = 1;
        for (int v : cnt.values()) {
            ans *= v + 1;
        }

        return ans - 1;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int solution(vector<vector<string>> clothes) {
    unordered_map<string, int> cnt;
    for (auto& c : clothes) cnt[c[1]]++;

    int ans = 1;
    for (auto& [_, v] : cnt) {
        ans *= v + 1;
    }

    return ans - 1;
}
```

```python
import math
from collections import Counter


def solution(clothes):
    cnt = Counter(kind for _, kind in clothes)
    return math.prod(v + 1 for v in cnt.values()) - 1
```

---

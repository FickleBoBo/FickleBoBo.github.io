---
title: "[백준] 10810번 - 공 넣기 [Java][C++]"
date: 2025-12-02
categories: [PS, BaekJoon]
tags: [구현]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/10810)

---

## 1. 문제 풀이

주어진 바구니들에 대해 주어진 구간에 특정 번호의 공을 넣는 과정을 반복할 때, 최종적으로 바구니에 들어있는 공의 번호를 출력하는 문제다. 바구니들을 배열로 간주하고 주어진 구간에 공의 번호를 덮어씌우는 과정을 반복하면 해결할 수 있다.

---

## 2. 코드

### 1. 구현 [Java]

`Arrays.fill` 메서드로 주어진 구간을 공의 번호로 채우는 방식으로 구현했다. 이때 첫 인덱스가 $0$ 인 것과 `Arrays.fill` 메서드는 시작 위치는 포함하고 끝 위치는 포함하지 않는 것에 주의해야 한다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int M = Integer.parseInt(st.nextToken());

        int[] arr = new int[N];
        for (int i = 0; i < M; i++) {
            st = new StringTokenizer(br.readLine());
            int start = Integer.parseInt(st.nextToken());
            int end = Integer.parseInt(st.nextToken());
            int num = Integer.parseInt(st.nextToken());

            Arrays.fill(arr, start - 1, end, num);
        }

        for (int n : arr) {
            sb.append(n).append(" ");
        }

        System.out.println(sb);
    }
}
```

### 2. 구현 [C++]

`fill` 함수를 활용해 벡터에서 해당 구간에 공의 번호를 넣어줬다. 이때 첫 인덱스가 $0$ 인 것과 `fill` 함수는 시작 위치는 포함하고 끝 위치는 포함하지 않는 것에 주의해야 한다.

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m;
    cin >> n >> m;

    vector<int> v(n);
    for (int i = 0; i < m; i++) {
        int s, e, x;
        cin >> s >> e >> x;

        fill(v.begin() + s - 1, v.begin() + e, x);
    }

    for (int x : v) {
        cout << x << ' ';
    }
}
```

---

## 3. 풀이 정보

### 1. 구현 [Java]

| 언어    | 시간   | 메모리   | 코드 길이 |
| ------- | ------ | -------- | --------- |
| Java 11 | 104 ms | 14264 KB | 893 B     |

### 2. 구현 [C++]

| 언어   | 시간 | 메모리  | 코드 길이 |
| ------ | ---- | ------- | --------- |
| C++ 17 | 0 ms | 2020 KB | 370 B     |

---

---
title: "[백준] 2960번 - 에라토스테네스의 체 [Java]"
date: 2025-11-27
categories: [PS, BaekJoon]
tags: [수학, 정수론, 소수 판정, 에라토스테네스의 체]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/2960)

![](/assets/posts/2025-11/백준%202960%20에라토스테네스의%20체/photo1.png)
![](/assets/posts/2025-11/백준%202960%20에라토스테네스의%20체/photo2.png)

---

## 문제 풀이

에라토스테네스의 체에서 탐색한 수들을 카운팅하면 되는 문제로 에라토스테네스의 체 로직 중간에 탐색에 거쳐간 값들을 카운팅하는 방식으로 해결했다. 이때 에라토스테네스의 체는 중복 탐색을 하므로 방문 체크를 통해 방문한 적 없는 값들만 카운팅 해야 한다.

---

## 코드

### 1. 에라토스테네스의 체 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int K = Integer.parseInt(st.nextToken());

        System.out.println(sieve(N, K));
    }

    // 에라토스테네스의 체
    private static int sieve(int N, int K) {
        boolean[] isPrime = new boolean[1 + N];
        Arrays.fill(isPrime, 2, N + 1, true);
        int cnt = 0;

        for (int i = 2; i <= N; i++) {
            if (isPrime[i]) {
                cnt++;
                if (cnt == K) return i;

                for (int j = i * i; j <= N; j += i) {
                    if (isPrime[j]) {
                        isPrime[j] = false;

                        cnt++;
                        if (cnt == K) return j;
                    }
                }
            }
        }

        return -1;
    }
}
```

---

## 풀이 정보

### 1. 에라토스테네스의 체 [Java]

- 7 min

![](/assets/posts/2025-11/백준%202960%20에라토스테네스의%20체/photo3.png)

---

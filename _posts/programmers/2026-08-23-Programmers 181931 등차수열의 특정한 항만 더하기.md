---
title: "[Programmers] #181931 - 등차수열의 특정한 항만 더하기 [Java][C++][Python]"
date: 2026-08-23
categories: [PS, Programmers]
tags: ["warm up"]
description: "등차수열에서 boolean 배열로 지정된 항들만 골라 합산하는 워밍업 문제."
slug: programmers-181931
media_subpath: /assets/img/posts/programmers-181931/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181931)
{: .prompt-info }

---

## 1. 아이디어

첫째항 `a`, 공차 `d`인 등차수열의 $(i + 1)$번째 항은 $a + d \times i$($i$는 0-indexed)로 바로 구할 수 있다. `included` 배열을 순회하면서 `included[i]`가 `true`인 위치의 항만 이 식으로 계산해 더하면 된다.

---

## 2. 복잡도

| 접근 | 시간   | 공간   |
| ---- | ------ | ------ |
| 풀이 | $O(N)$ | $O(1)$ |

($N$ = `included`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
class Solution {
    public int solution(int a, int d, boolean[] included) {
        int sum = 0;
        for (int i = 0; i < included.length; i++) {
            if (included[i]) sum += a + d * i;
        }

        return sum;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

int solution(int a, int d, vector<bool> included) {
    int sum = 0;
    for (int i = 0; i < included.size(); i++) {
        if (included[i]) sum += a + d * i;
    }

    return sum;
}
```

```python
def solution(a, d, included):
    return sum(a + d * i for i, flag in enumerate(included) if flag)
```

---

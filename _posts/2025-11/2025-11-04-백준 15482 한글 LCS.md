---
title: "[백준] 15482번 - 한글 LCS [Java]"
date: 2025-11-04
categories: [PS, BaekJoon]
tags: [문자열, 다이나믹 프로그래밍, LCS]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/15482)

![](/assets/posts/2025-11/백준%2015482%20한글%20LCS/photo1.png)
![](/assets/posts/2025-11/백준%2015482%20한글%20LCS/photo2.png)

---

## 문제 풀이

[백준 9251번 - LCS]({% post_url 2025-11/2025-11-04-백준 9251 LCS %}) 문제에서 문자열만 한글로 바뀐 문제다.

---

## 코드

### 1. Bottom-Up dp [Java]

`BufferedReader`를 통해 입력을 받으면 UTF-8 인코딩을 알아서 해주므로 동일하게 해결할 수 있다.

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] str1 = br.readLine().toCharArray();
        char[] str2 = br.readLine().toCharArray();
        int N = str1.length;
        int M = str2.length;

        int[][] dp = new int[1 + N][1 + M];
        for (int i = 1; i <= N; i++) {
            for (int j = 1; j <= M; j++) {
                if (str1[i - 1] == str2[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                } else {
                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }

        System.out.println(dp[N][M]);
    }
}
```

### 2. Top-Down dp [Java]

`BufferedReader`를 통해 입력을 받으면 UTF-8 인코딩을 알아서 해주므로 동일하게 해결할 수 있다.

```java
import java.io.*;

public class Main {

    private static int[][] dp;
    private static boolean[][] visited;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] str1 = br.readLine().toCharArray();
        char[] str2 = br.readLine().toCharArray();
        int N = str1.length;
        int M = str2.length;

        dp = new int[N][M];
        visited = new boolean[N][M];

        System.out.println(lcs(str1, N - 1, str2, M - 1));
    }

    private static int lcs(char[] str1, int len1, char[] str2, int len2) {
        if (len1 == -1 || len2 == -1) return 0;

        if (visited[len1][len2]) return dp[len1][len2];

        visited[len1][len2] = true;
        if (str1[len1] == str2[len2]) {
            return dp[len1][len2] = lcs(str1, len1 - 1, str2, len2 - 1) + 1;
        } else {
            return dp[len1][len2] = Math.max(lcs(str1, len1 - 1, str2, len2), lcs(str1, len1, str2, len2 - 1));
        }
    }
}
```

---

## 풀이 정보

### 1. Bottom-Up dp [Java]

- 2 min

![](/assets/posts/2025-11/백준%2015482%20한글%20LCS/photo3.png)

### 2. Top-Down dp [Java]

- 3 min

![](/assets/posts/2025-11/백준%2015482%20한글%20LCS/photo4.png)

---

---
name: review
description: PS 포스트를 꼼꼼히 검수하고 등급(S/A/B/C)을 매긴다. 기계검사 → 청크별 Sonnet 에이전트 분석 → 메인 에이전트 등급 판정 → 미기입 복잡도 표 채우기(확인 후). 아이디어가 전부 비어 있으면 등급 없이 코드·복잡도만 보는 선검사 모드로 자동 전환된다. "/review" 또는 "/review 120807"처럼 호출한다.
---

# /review — PS 포스트 검수

PS 포스트를 **누락 없이 꼼꼼히** 점검하고 등급을 매긴다(무인자면 `_drafts/` 전체,
번호를 주면 `_drafts/` 우선·없으면 `_posts/`).
등급은 발행 게이트다: **S/A면 믿고 바로 `/publish`, B는 개선 권장, C는 수정 필수.**

원칙:
- **파일을 수정하지 않는다.** 검수 결과만 보고한다. **단 하나의 예외가 미기입 복잡도 표**로,
  Phase 3.5에서 확인을 받아 채운다(아래). 그 외 어떤 내용도 고치지 않는다.
- **역할 분담(중요):** 한 번에 많은 포스트를 몰아 보면 뒤쪽이 부실해지므로, **청크별 독립 Sonnet 에이전트가
  분석(analysis)을 실질적으로 짊어진다** — 아이디어·코드·복잡도·구조를 각 청크 신선한 컨텍스트에서 꼼꼼히.
  **메인은 판정(judgment)을 맡는다** — 에이전트 분석을 1차 자료로 받아 등급을 매기되,
  **등급을 가르는 주장(코드 정확성·복잡도)은 원본으로 재확인**한다.
- 메인은 **전부 재trace 하지 않고, 등급-결정적 부분만 타겟 재확인**한다.
  에이전트 출력을 통째로 받아쓰지도(rubber-stamp), 통째로 재수행하지도 않는다.

---

## 두 가지 모드 (자동 판별)

이 스킬은 파이프라인에서 **두 번** 돈다. 어느 쪽인지는 Phase 1의 기계 검사 결과로 갈리며,
사용자가 인자로 지정하지 않는다.

| | **선검사 모드** | **전체 리뷰** |
|---|---|---|
| 조건 | 대상 포스트가 *전부* `아이디어 비어있음` | 하나라도 아이디어가 채워져 있음 |
| 시점 | `/ps` 직후, 아이디어 쓰기 전 | 아이디어를 다 쓴 뒤 |
| 목적 | **접근 확정** — 코드가 이대로 갈 만한지 | **발행 게이트** — 등급 판정 |
| 등급 | **매기지 않는다** | 매긴다 |
| 복잡도 채우기 | 한다 | 한다 |

**왜 선검사가 필요한가:** 아이디어를 다 쓴 뒤에야 더 좋은 풀이가 발견되면, 그 풀이를 전제로 쓴
설명 문단이 통째로 버려진다(실제 사례: LeetCode 1번 `a11f788` — 중복값 케이스 분석 문단이
원패스 풀이로 바뀌며 삭제됨). 글을 쓰기 *전*이면 PS 레포 코드를 고치고 초안을 지운 뒤
`/ps`로 재생성하면 그만이라 비용이 0이다.

**선검사 모드에서 복잡도를 채우는 이유:** 복잡도는 코드에서만 나오는 값이라 아이디어와 무관하다.
여기서 확정해두면 사용자가 아이디어를 쓸 때 참고할 수 있고, 2회차는 검증만 하면 된다.
코드를 고쳤다면 초안을 지우고 `/ps`로 재생성하므로 표도 빈 상태로 돌아온다 — 낡은 값이 남지 않는다.
(초안을 지우지 않고 코드블록만 손으로 고치면 `fill`이 재채움을 거부하니, 그때는 재생성할 것.)

---

## Phase 1. 기계 검사 + 청크 계획

```
python3 .claude/skills/review/review_check.py $ARGUMENTS
```

- 인자 없으면 `_drafts/` 전체, 있으면 해당 번호(`_drafts/` 우선, 없으면 `_posts/`).
- 스크립트가 결정론적으로 처리: 빈 아이디어 / 미기입 복잡도 표 / **복잡도 기호 정의 없음** / 누락 섹션 / TODO 제목·링크 / 언어 태그 없는 코드블록 / **콜아웃 문제** / **수식 뒤 공백 없음**을 플래그하고, **청크 계획**(포스트 5개 또는 12,000자 중 먼저 닿는 쪽, 단일 포스트 미분할)을 출력한다.
- **PS 레포 drift도 같은 실행에서 자동으로 잡는다** — `review_check.py`가 `../sync/sync.py`의
  `compute_drift()`를 그대로 import해 각 대상마다 돌린다(재구현 아님). 코드가 바뀜/새 풀이
  생김/언어 누락/PS 레포 쪽 언어 소실이 있으면 `/sync`와 동일한 diff 리포트가 기계검사 출력에
  섞여 나온다. **읽기 전용** — 여기서 고치지 않는다. 드리프트가 나오면 Phase 2로 넘어가기 전에
  `/sync`(apply까지)로 먼저 반영할지 사용자에게 확인한다. 안 그러면 청크 에이전트가 이미 낡은
  코드를 리뷰하게 된다.
- 기계 검사 결과를 사용자에게 보고한다.
- **모드 판별**: 대상 포스트가 *전부* `아이디어 비어있음`이면 **선검사 모드**, 하나라도 채워져
  있으면 **전체 리뷰**다. 스크립트 출력으로 결정되는 사실이므로 추측하지 않는다. 어느 모드로
  가는지 사용자에게 한 줄 알린다. 혼재(일부만 빔)는 전체 리뷰이며, 빈 포스트는 종전대로 C다.

### 콜아웃·수식 공백은 반드시 전달한다

`콜아웃 깨짐` / `콜아웃 prettier-ignore 누락` / `수식 뒤 공백 없음` 셋은 **사용자가 눈으로
못 잡는 종류**다 — 렌더링이 깨지는 게 아니라 조용히 어긋나고, 무관한 파일을 저장하다 딸려
들어온다. 스크립트가 고치는 법과 행 번호까지 출력하므로 **그대로 전달하고 그냥 넘기지 말 것.**
(원리는 `review_check.py`의 `_callout_issues`·`_math_spacing_issues` docstring에 있다.)

## Phase 2. 청크 계획 확인 → Sonnet 에이전트

1. 청크 계획(총 포스트 수, 청크 수 = 띄울 에이전트 수)을 사용자에게 알리고 **확인을 받는다**(에이전트 다수 = 비용).
2. **청크마다 Sonnet Agent 하나**를 띄운다:
   - `subagent_type: "general-purpose"`, `model: "sonnet"`, `run_in_background: true`.
   - `description`에 청크 번호 포함 (예: `review chunk 2/3`).
   - 청크가 여러 개면 전부 병렬로 띄우고 완료를 기다린다(자동 깨어남).
3. 분석 지침은 **`agent_prompt.md`가 단일 진실 소스**다. 프롬프트에 복사하지 말고 경로만 넘긴다 —
   옮겨 적으면 요약·누락이 생기고, 청크 수만큼 같은 내용이 중복 전송된다. 프롬프트는 이게 전부다:

```
먼저 .claude/skills/review/agent_prompt.md 를 읽고, 그 지침을 그대로 따라
아래 포스트를 분석한다. (경로는 블로그 레포 루트 기준)

{청크의 파일 절대경로 목록}
```

## Phase 3. 메인 에이전트 판정

모든 에이전트 완료 후, 메인이 각 포스트에 등급을 부여한다. **에이전트 분석을 1차 자료로 신뢰하되,
등급을 가르는 주장은 원본으로 재확인한다** — 통째로 받아쓰지도(rubber-stamp), 통째로 재수행하지도(전체 재trace) 않는다.

**선검사 모드에서는 등급만 생략하고, 원본 대조는 그대로 한다.** 아이디어가 비어 있으니 등급은
전부 C로 확정되어 정보량이 0이다. 반면 아래 (b)·(d)의 원본 대조는 선검사에서 *더* 중요하다 —
여기서 나온 "코드가 틀렸다" · "더 좋은 풀이가 있다"는 주장이 곧 **PS 레포 코드 재작성**이라는
비싼 행동으로 이어지기 때문이다. 에이전트 말만 믿고 멀쩡한 풀이를 갈아엎게 만들면 안 된다.
아래 HARD STOP 1번의 취지는 등급이 아니라 그 검증에 있다. (c) 아이디어 평가만 건너뛴다.

### 절대 금지 (HARD STOP)
1. **코드 정확성·복잡도를 원본 확인 없이 에이전트 말만으로 등급** — 이 둘은 등급을 직접 가르므로 반드시 원본 대조.
   (에이전트가 "정확/지적 없음"이라 해도 코드·복잡도는 메인이 원본을 본다.)
2. 에이전트 "지적 없음" → 그대로 상위 등급 (rubber-stamp)
3. 기억으로 문제/복잡도 판단 — 불확실하면 확인(WebFetch)
4. 같은 시리즈라고 일괄 동일 등급
5. 분포 맞추려 등급 인위 조정

### 판정 절차 (파일마다)
- **(a) 에이전트 분석 정독** — 그 포스트에 대한 아이디어·코드·복잡도·구조 지적을 먼저 읽는다.
- **(b) 등급-결정적 재확인 (원본 대조)** — 등급을 가르는 지점만 원본으로 직접 확인한다. 전체 라인 trace가 아니라 집중 확인:
  - **코드 정확성**: 핵심 로직·루프 경계·타입·명백한 엣지(N=1·빈 입력·오버플로우)를 원본 코드로 확인. 에이전트의 정확/버그 주장을 검증.
  - **복잡도**: 표가 차 있으면 값이 코드와 맞는지 원본 대조. **표가 비어 있으면 원본 코드로 값을 확정한다**
    — 이 값이 Phase 3.5에서 파일에 기록된다. 기준은 `agent_prompt.md`의 **공간복잡도 관례**를
    그대로 쓴다(표를 확정해야 하면 그 파일을 읽는다 — 관례를 기억으로 재구성하지 말 것).
    관례로도 갈리는 경우만 단정을 피해 사용자에게 판단을 넘긴다.
  - 에이전트 값과 메인 값이 **다르면 자동으로 한쪽을 택하지 말고 양쪽을 병기해 사용자에게 묻는다.**
    (같은 대화 안이라 두 계산이 완전히 독립적이진 않다. 그래도 값이 갈렸다는 사실 자체는 확인 가치가 있다.)
- **(c) 아이디어 비판 평가** — 에이전트 분석 + 필요시 원본으로, 핵심 통찰을 설명하는지 판단. 비-trivial인데 한 줄이면 보강 필요.
- **(d) 대안 풀이 판단** — 에이전트가 제시한 대안이 진짜 점근적 개선(예: $O(N^2)\to O(N\log N)$)인지, 아니면
  취향·미세 최적화 수준인지 가른다. 전자는 "진짜 개선점"으로 B 사유가 될 수 있고(등급 정의 참고),
  후자는 등급에 영향 없이 개선점 노트로만 남긴다. 42576/1845 사례처럼 관용·동치인데 에이전트가
  과하게 지적한 경우 기각하고 근거를 보고에 남긴다.
- (b)에서 에이전트 분석과 원본이 어긋나면 **원본을 따르고**, 에이전트가 놓쳤거나 과했음을 보고에 남긴다.

### 등급 정의
| 등급 | 기준 |
|------|------|
| **S** | 완벽. 뛰어난 설명 + 코드. trivial 문제엔 거의 없음 |
| **A** | 깔끔. 정확한 코드·적절한 설명·진짜 흠 없음 |
| **B** | 무난하나 *진짜* 개선점 있음(vague한 핵심 단계, 복잡도 불일치, 핵심 누락) |
| **C** | 수정 필수(코드 오류, idea 명백한 오류, 미기입 스켈레톤) |

- **복잡도 표 미기입: `fill`이 성공했으면 등급에 반영하지 않는다**(등급은 *채운 뒤 상태* 기준).
  **빈 채로 남았으면 종전대로 C다** — 3.5를 건너뛰었든, `fill`이 거부했든, 사용자가 승인하지 않았든
  마찬가지. `/publish`는 복잡도를 검사하지 않으므로 **등급이 유일한 방벽이다.** 판단 근거는 `fill`의
  성공 출력이지, 채웠다는 기억이 아니다.
- 사용자가 직접 적은 값이 코드와 **틀린** 경우는 채우기 대상이 아니라 지적 대상이고 B 사유다.
  (`fill`도 이미 채워진 표는 거부한다.)
- 코드 버그(오답·오버플로우·타입) → **C**
- 핵심 근거 누락으로 포스트만으로 이해 불가 → **B 이하**
- 사소한 지적(변수명 등)만 → **A** 유지
- 기본값은 "A 자격을 *증명*"이지 "흠 없으면 A"가 아니다.

### 재리뷰 일관성
같은 대화에서 파일 미변경 → 이전 등급 유지. 변경하려면 근거 필수(수정으로 지적 해소 / 관례 변경).

## Phase 3.5. 복잡도 표 채우기 + 기호 정의

Phase 1이 `복잡도 표 미기입`·`복잡도 기호 정의 없음` 어느 쪽도 플래그하지 않았으면 건너뛴다.
(값은 이미 차 있고 정의만 없는 경우도 있다 — 그때는 4번만 실행한다.)
**선검사 모드에서도 똑같이 실행한다** — 복잡도는 코드에서만 나오는 값이라 아이디어와 무관하다.
단 이 단계 뒤에 코드를 고치기로 했다면 초안을 지우고 `/ps`로 재생성해, 채운 값이 아니라 빈 표에서
다시 시작한다(`fill`은 이미 채워진 표를 거부한다).

1. **확인받는다.** Phase 3에서 확정한 값을 한 표로 모아 보여준다(근거를 붙여 눈으로 검증되게).
   에이전트 값과 메인 값이 갈렸으면 **양쪽을 병기**하고 어느 쪽을 쓸지 묻는다.
   ```
   | 문제 | 풀이 | 시간 | 공간 | 근거 |
   |------|:----:|:----:|:----:|------|
   ```
2. **승인 후 포스트마다 실행한다.** 접근이 여러 개면 `### 1. 풀이`, `### 2. 풀이` 순서대로 쌍을 넘긴다.
   ```
   python3 .claude/skills/review/review_check.py fill <번호> <시간> <공간> [<시간> <공간> ...]
   ```
   값은 LaTeX로 넘긴다(`"O(N \log N)"`). `$…$`는 스크립트가 붙이므로 있어도 없어도 된다.
3. **직접 Edit하지 않는다.** 파일 쓰기는 전부 스크립트가 한다 — 빈 행 위치 판별, 개수 검증,
   순서 대응, 이미 채워진 표·발행본 거부, 기록 후 재검사까지. 거부 메시지가 나오면 그대로 사용자에게
   전달하고 멈춘다. 우회하려 Edit을 쓰지 말 것.

4. **기호 정의를 이어서 쓴다.** 값만 채우면 표가 안 읽힌다 — 42748이 `$O(MN \log N)$`으로
   발행됐는데 $M$·$N$이 본문 어디에도 없어 해석 불가능한 상태였다. `fill`이 끝나면
   `→ 다음: … vars …`를 안내하므로 그대로 실행한다.
   ```
   python3 .claude/skills/review/review_check.py vars <번호> "<정의>" ["<정의2>" ...]
   ```
   - **표마다 하나씩**, 접근 순서대로 넘긴다. 여러 접근이 기호를 공유해도 각 표가 자립하도록
     반복해서 적는다(독자가 위로 스크롤하지 않게).
   - 기호가 코드의 다른 변수와 충돌하면 그 점까지 적는다 — 120583은 매개변수 `n`(찾을 값)과
     $N$(배열 길이)이 대소문자만 다르고 뜻이 정반대라 혼동을 부른다.
   - `fill`과 달리 **발행본에도 쓸 수 있다.** 값을 바꾸는 게 아니라 읽기 보조를 더하는 것이라서다.
   - 정의 개수 대응·기호 누락·기호 없는 표($O(1)$뿐)·빈 표는 스크립트가 검증해 거부하므로
     미리 따질 필요 없다. 거부 메시지를 그대로 읽고 고치면 된다.

   출력 형식(스크립트가 표 바로 아래에 blockquote로 삽입):
   ```
   | 시간복잡도 | 공간복잡도 |
   | :-: | :-: |
   | $O(MN \log N)$ | $O(N)$ |

   > $N$ = `array` 길이, $M$ = `commands` 개수
   ```
   IAL이 없는 순수 blockquote라 Prettier 콜아웃 손상 문제와 무관하다.

## Phase 4. 결과 보고
1. **에이전트 분석을 청크 순서대로, 요약하지 말고 원문 그대로 전달한다.**
   메인이 압축하면 사용자는 에이전트가 *실제로 무엇을 검증했는지*(트레이스한 반례, 확인한
   엣지 케이스, 복잡도 근거)를 볼 방법이 없어져 "한 줄 요약만 보인다"는 상태가 된다.
   에이전트 출력은 메인 컨텍스트에만 남고 사용자에겐 대조할 원본이 없으므로, 요약은
   검증 가능성을 통째로 없앤다. 메인의 판정 근거는 원문 **뒤에 덧붙이는** 것이지
   원문을 대체하지 않는다.
2. 등급표:
```
| # | 문제 | 등급 | 발행 판단 | 주요 지적사항 |
|---|------|:----:|----------|--------------|
```
   - 발행 판단: **S/A → 발행 가능**, **B → 개선 권장**, **C → 수정 필수**
3. B/C가 있으면 무엇을 고쳐야 하는지 요약.
4. Phase 3.5에서 쓴 게 있으면 **어느 포스트에 무슨 값·무슨 기호 정의를 넣었는지 목록으로 남긴다**
   (사용자가 확인만 하면 되도록). 기록을 건너뛴 항목이 있으면 그 이유도 함께.

### 선검사 모드일 때

1번(원문 전달)과 4번(채운 복잡도 목록)은 동일하다. 2·3번 등급표 대신 **판단표**를 낸다 —
사용자가 결정할 것은 발행 여부가 아니라 **이 코드로 글을 써도 되는가**이기 때문이다.

```
| # | 문제 | 코드 | 대안 풀이 | 조치 |
|---|------|:----:|----------|------|
```
- 코드: **정확** / **버그**(반례 명시)
- 대안 풀이: **없음**(이대로 진행) / 제시된 접근 + 복잡도 비교
- 조치: **그대로 작성** / **코드 재작성 권장**(점근적 개선이 있을 때만) / **수정 필수**(버그)

대안이 취향·미세 최적화 수준이면 조치는 `그대로 작성`이고, 개선점 노트로만 남긴다.
Phase 3 (d)의 기준을 그대로 쓴다 — 여기서 `재작성 권장`을 남발하면 사용자가 멀쩡한 풀이를
갈아엎게 된다.

마지막에 다음 단계를 안내한다.
- 전부 `그대로 작성` → **아이디어를 채운 뒤 `/review` 재실행**(이때 전체 리뷰로 자동 전환된다).
- 재작성할 게 있으면 → PS 레포 코드 수정 → 해당 초안 삭제 → `/ps` 재실행 →
  다시 선검사. **아이디어를 쓰기 전에** 이 루프를 닫는다.

---

## 태그
태그 부착은 **`/review`가 하지 않는다.** 별도 `/tag` 스킬(closed-vocab `tags.md`)이 담당하며,
파이프라인상 `/review` 다음에 사용자가 직접 실행한다. 따라서 이 스킬은 태그 추천·검증을 하지 않는다.

---
title: "[Programmers] #181893 - 배열 조각하기 [Java][C++][Python]"
date: 2026-08-29
categories: [PS, Programmers]
tags: ["warm up"]
slug: programmers-181893
media_subpath: /assets/img/posts/programmers-181893/
math: true
mermaid: false
---

<!-- prettier-ignore -->
> [문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/181893)
{: .prompt-info }

---

## 1. 아이디어

정수 배열 `query`에 대해 인덱스가 짝수면 `arr`를 0번째부터 `query[i]`번째까지 남기고, 홀수면 `query[i]`번째부터 끝까지 남기는 과정을 반복하면 된다. 각 단계마다 남은 배열을 새롭게 만들어 `arr`에 대입하면 깔끔하게 연속된 쿼리를 처리할 수 있다.

---

## 2. 복잡도

| 접근 | 시간            | 공간   |
| ---- | --------------- | ------ |
| 풀이 | $O(N \times Q)$ | $O(N)$ |

($N$ = `arr`의 길이, $Q$ = `query`의 길이)

---

## 3. 코드

### 풀이 [Java][C++][Python]

```java
import java.util.*;

class Solution {
    public int[] solution(int[] arr, int[] query) {
        for (int i = 0; i < query.length; i++) {
            if (i % 2 != 0) {
                arr = Arrays.copyOfRange(arr, query[i], arr.length);
            } else {
                arr = Arrays.copyOfRange(arr, 0, query[i] + 1);
            }
        }

        return arr;
    }
}
```

```c++
#include <bits/stdc++.h>
using namespace std;

vector<int> solution(vector<int> arr, vector<int> query) {
    for (int i = 0; i < query.size(); i++) {
        if (i % 2) {
            arr = vector<int>(arr.begin() + query[i], arr.end());
        } else {
            arr = vector<int>(arr.begin(), arr.begin() + query[i] + 1);
        }
    }

    return arr;
}
```

```python
def solution(arr, query):
    for i, q in enumerate(query):
        arr = arr[q:] if i % 2 else arr[: q + 1]

    return arr
```

---
